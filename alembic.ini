[alembic]
# path to migration scripts (moved under app/db to keep db code together)
script_location = app/db/migrations
# The runtime DB URL is provided by the application settings at runtime.
# `app.core.config.settings.database_url` when available.
sqlalchemy.url = driver:///

[alembic:runtime]
sqlalchemy.url = driver:///
